{
  "name": "test-pilot-metrics-sdk-example",
  "id": "sdk-example@testpilot-metrics",
  "version": "0.0.1",
  "description": "A tiny example SDK add-on that uses testpilot-metrics to ping GA each time a toolbar button is clicked",
  "main": "index.js",
  "author": "Mozilla (https://mozilla.org)",
  "engines": {
    "firefox": ">=38.0a1"
  },
  "license": "MPL-2.0",
  "keywords": [
    "jetpack"
  ],
  "scripts": {
    "build": "cp ../../testpilot-metrics.js . && jpm xpi"
  },
  "devDependencies": {
    "jpm": "^1.2.2"
  }
}
